---
name: identity
description: Show current identity state — soul, self-state, anchors, and observed patterns
---

# Identity Check

Load and display your current identity state.

## Steps

1. Call `identity:self` to retrieve current state
2. Display the results — soul, self-state, anchors, and top observed patterns
3. Note any patterns close to promotion threshold

This is the same data loaded automatically at session start, but useful for checking mid-session or after reflecting.
